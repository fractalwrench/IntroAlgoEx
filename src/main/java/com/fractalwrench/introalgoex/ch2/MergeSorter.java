package com.fractalwrench.introalgoex.ch2;

/**
 * Merge sort is a divide and conquer algorithm which recursively splits an array up into subarrays,
 * sorts the values, then combines the subarrays. It does not sort the values in place.
 *
 * Runs in O(nlog(n)) time
 */
public class MergeSorter implements IntSorter {

    @Override
    public void sort(int[] values) {
    }

}
